# db.py
import json
import os
from config import ARCHIVO_VEHICULOS, ARCHIVO_RESERVAS

def cargar_datos(archivo, tipo):
    # ... (código completo de cargar_datos)

def guardar_datos(archivo, datos):
    # ... (código completo de guardar_datos)
